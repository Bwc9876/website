---
import Layout from "@layouts/Layout.astro";
import ProjectGrid from "@components/projects/ProjectGrid.astro";
import { getCollection } from "astro:content";

const projectEntries = (await getCollection("projects")).length;
---

<Layout title="Projects" description="Ben C's Projects">
  <hgroup>
    <h1 class="gradient-text">My Projects</h1>
    <p>Sorted By Year • {projectEntries} Total</p>
  </hgroup>
  <ProjectGrid />
</Layout>

<style>
  h1 {
    margin-bottom: var(--small);
  }

  p {
    font-size: var(--3);
    margin-top: 0;
  }
</style>
